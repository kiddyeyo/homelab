http:
  routers:
    rustfs-rtr:
      rule: "Host(`rustfs.infra.sintaq.net`)"
      entryPoints: ["websecure"]
      service: rustfs-svc
      tls:
        certResolver: cloudflare

  services:
    rustfs-svc:
      loadBalancer:
        servers:
          - url: "http://10.24.10.6:9001"
