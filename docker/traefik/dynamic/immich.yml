http:
  routers:
    immich-rtr:
      rule: "Host(`immich.infra.sintaq.net`)"
      entryPoints: ["websecure"]
      service: immich-svc
      tls:
        certResolver: cloudflare

  services:
    immich-svc:
      loadBalancer:
        servers:
          - url: "http://:2283"
