http:
  routers:
    rustfs-api-rtr:
      rule: "Host(`s3.infra.sintaq.net`)"
      entryPoints: ["websecure"]
      service: rustfs-api-svc
      tls:
        certResolver: cloudflare

  services:
    rustfs-api-svc:
      loadBalancer:
        servers:
          - url: "http://10.24.10.6:9000"
