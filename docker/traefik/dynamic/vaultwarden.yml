http:
  routers:
    vaultwarden-rtr:
      rule: "Host(`vaultwarden.infra.sintaq.net`)"
      entryPoints: ["websecure"]
      service: vaultwarden-svc
      tls: {}

  services:
    vaultwarden-svc:
      loadBalancer:
        servers:
          - url: "http://10.24.10.11:80"
