---
- name: "Recrear stack de filebrowser"
  ansible.builtin.command: "docker compose up -d --force-recreate --remove-orphans"
  args:
    chdir: "{{ stack_base_path }}/filebrowser"
  changed_when: true
